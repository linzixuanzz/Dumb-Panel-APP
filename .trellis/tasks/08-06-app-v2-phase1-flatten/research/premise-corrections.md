# 六个探测对我 brief 的前提更正

## radius collapse (11 distinct values -> 3 tiers + pill)  (50 条具体改动)

- Site count is understated. `BorderRadius.circular(<literal>)` matches 140 lines, but ONE of those (app_card.dart:13) is inside a `///` doc comment, so only 139 are live code. Against that, 8 more radius decisions exist in forms the regex cannot see: script_list_page.dart:2466 (999, multi-line), subscription_list_page.dart:1496 (999, multi-line), app_theme.dart:249 (20, BorderRadius.vertical), task_form_page.dart:798-799 (14, BorderRadius.vertical), task_cron_list.dart:37/69/80 (compact ternaries), app_card.dart:64 (param-driven). Plus 3 already tokenized (app_buttons.dart:37/107, app_card.dart:52). True scope ≈150 sites, matching the '149' figure the design_tokens.dart header comment already records at line 4.
- There are 17 literal pill sites, not 15 — the two missing ones are multi-line (script_list_page.dart:2466, subscription_list_page.dart:1496). Counting app_buttons.dart:37 the total is 18.
- The brief expected some r=999 sites to be 'a large rounded card pretending to be a pill'. I read all 17 and there are ZERO such cases — every one is a real chip (h 19-40), a progress bar (h 6-8, r clamps), or a literal BoxShape.circle. The actual anomaly runs the other direction: four sites already render as pills without saying so — dep_list_page.dart:1707 (r=20, h≈31), dep_list_page.dart:1732 (r=10, h≈15), app_theme.dart:244 chipTheme (r=20 on a 32px Chip), and dashboard_page.dart:603 (r=4 on a 6px LinearProgressIndicator). Those four are where the honesty fix belongs, and all four are zero-pixel changes.
- The r=4 sites are not 'progress bars / code blocks where 4 is already minimal'. Six of nine are micro TEXT BADGES at fontSize 10 with 1-3px vertical padding (user_list:658, open_api:401, open_api:956, open_api:1201, subscription_list:1150, dep_list:1476) and can all move to sm. Two are Material `Checkbox.shape` (login_page.dart:678, log_list_page.dart:775) — a control, not a surface, where folding into sm=6 makes them ROUNDER, the opposite of the goal; they need a separate `control` token. Exactly one is a progress-bar clip (dashboard_page.dart:603). There are ZERO code blocks at r=4 — the actual code/log blocks are at 8 (open_api:1041), 12 (script_list:2260, subscription:1381) and 16 (script_list:2812, panel_log:181).
- 'only 9 AppCard( matches so far' overcounts. There are 6 live AppCard widget usages, all in one file: system_settings_page.dart:582, 629, 688, 746, 829, 843. The other 3 substring matches are `_buildAppCard(` at open_api_page.dart:173 and :351 (an unrelated private method) and the constructor declaration at app_card.dart:47.
- 'only 6 sites use Flutter Card, so cardTheme barely matters' — the 6 count is right (resource_card.dart:20, server_config_page.dart:355, backup_page.dart:923/1025/1206/1399) but the conclusion is too strong: three of them are the full-width structural panels of backup_page.dart, so cardTheme visibly governs that entire page. Low blast radius, not zero.
- The 48-site r=12 bucket cannot be remapped as a unit. Roughly 17 of the 48 are `OutlineInputBorder` copies that duplicate app_theme.dart:149/153/157 verbatim and should be DELETED rather than tokenized (task_list:611/617/623, log_list:535/541/547, env_list:879/887/895, subscription:229/237/245, plus app_theme's own 3). Another ~14 are outer card surfaces that belong in the lg tier, not md (more_page:539, notification:912, security:287/561/808, dep_list:1418, env_list:981/1983, dashboard:641, task_list:2184, system_settings:1155, login:630, open_api:1183). The remainder are genuine md controls/inner boxes.

## shadow removal (boxShadow + all elevation/shadowColor sources)  (16 条具体改动)

- Your brief asks about 'FloatingActionButton ... elevation in app_theme.dart'. There is no FloatingActionButton anywhere in lib/ — grep for `FloatingActionButton|floatingActionButton` returns zero matches — and app_theme.dart has no floatingActionButtonTheme. Nothing to fix.
- 'AppBar elevation' is already handled: app_theme.dart:128-129 sets `elevation: 0` AND `scrolledUnderElevation: 0`. Nothing to fix.
- 'BottomNavigationBar elevation in app_theme.dart': the navigationBarTheme block at app_theme.dart:214-237 (24 lines, including elevation: 0) is DEAD CODE. Grep for `NavigationBar(|BottomNavigationBar(|NavigationRail(` returns zero matches. The real bottom nav is hand-rolled at shared/widgets/main_scaffold.dart:64-129 — a BackdropFilter + Container with a 0.5px top BorderSide (lines 72-77) and no shadow. It is already flat. The dead theme block is a separate cleanup candidate (delete lines 214-237).
- Dialogs and bottom sheets cast NO shadow, contrary to what a shadow audit usually assumes. I checked the pinned SDK at D:/flutter-nospace: _DialogDefaultsM3 (dialog.dart:1793-1816) sets `shadowColor => Colors.transparent` despite elevation 6.0, and _BottomSheetDefaultsM3 (bottom_sheet.dart:1437-1456) likewise sets `shadowColor => Colors.transparent`. So all 56 showDialog and 17 showModalBottomSheet call sites are already shadow-free and need no work.
- All 6 real Flutter `Card(` widgets (server_config_page.dart:355, resource_card.dart:20, backup_page.dart:923/1025/1206/1399) are already elevation 0 via cardTheme (app_theme.dart:139). The 52 'Card(' text matches in your baseline are overwhelmingly private widget class names (_UserCard, _TaskCard, _DepCard, _SubCard, _EnvCard, _StatCard, _MethodCard, _SponsorCard, TaskStatsCard, _buildCountCard, _buildBackupCard, ...), not Flutter Card widgets — the real count is 6.
- Minor arithmetic note on the baseline: '24 raw BoxShadow text matches = 12 property + 12 constructor' only holds under a case-INSENSITIVE grep. `boxShadow:` (lowercase b) does not match the literal `BoxShadow`. Case-sensitive counts are 12 `boxShadow:` and 12 `BoxShadow(`, which independently confirms 12 distinct sites — your number is right.
- No PhysicalModel, no Canvas.drawShadow, no TextStyle `shadows:`, and no kElevationToShadow usage anywhere in lib/. The shadow surface area is fully enumerated by this report.

## Decorative elements on dashboard + login ("花里胡哨")  (19 条具体改动)

- dashboard_page.dart is 665 lines, not 637.
- The gauge premise is half wrong. The ring gauge exists (resource_card.dart:34-48, a CircularProgressIndicator with the same percentage repeated as centered Text at L43) but it is DEAD CODE — grep for `ResourceCard|resource_card` across all of lib/ returns only the class declaration (resource_card.dart:3) and its constructor (:9), zero instantiations. It is not on screen. So the question 'what replaces the information if you remove the gauge' has no answer needed: delete the 76-line file outright. No user has ever seen it.
- 'Motion without meaning' — no explicit animation exists. Grep for AnimatedContainer|CustomPainter|CustomPaint|AnimationController|TweenAnimationBuilder|AnimatedOpacity|AnimatedSwitcher|FadeTransition|ScaleTransition|SlideTransition|AnimatedBuilder|RotationTransition|Transform. across lib/features/dashboard returns nothing, and the same grep across lib/features/login returns nothing. There is exactly one piece of unmeaning motion and it is implicit: dashboard_page.dart:605 passes `value: null` when memory is unavailable (call site L283-285), making the LinearProgressIndicator indeterminate — an infinitely looping sweep used to express 'no data'.
- The '装饰圆' is worse than decorative, it is a rendering artifact. The Stack at dashboard_page.dart:422 declares no clipBehavior, so it takes Flutter's default Clip.hardEdge, and it sizes to its non-positioned Column child, which lives inside the Container's EdgeInsets.all(20) (L408). The circle at top:-30/right:-30 is therefore hard-clipped along the content box, appearing as a blob with two straight cut edges roughly 20px inside the card's rounded border — not a soft circle bleeding off the corner as the code intends.
- The 8x8 dot at dashboard_page.dart:442-455 is not a status indicator. Its colour is hardcoded AppColors.primary (L446) with no binding to any connection, health, or error state — it is always 'on'. It also carries the wrong semantic colour: AppColors.info is defined as exactly primary (app_theme.dart:56) and is the app's '运行中/进行中' colour, while 'online' in this design system is AppColors.success #67C23A (app_theme.dart:47). So a decorative glowing dot is currently signalling 'running'.
- The shadow baseline undercounts. login_page.dart:528-529 is `elevation: 4` + `shadowColor: AppColors.primary.withAlpha(80)` — a coloured glow under the login button that is Material elevation, not a BoxShadow, so it is invisible to the 'BoxShadow' text grep behind the '12 boxShadow sites' figure. Any Phase 1 shadow sweep must also grep for `elevation:` and `shadowColor:`.
- The dashboard's real duplicated number is not a gauge, it is arithmetic. task_stats_card.dart:50-74 presents 总任务 / 已启用 / 运行中 / 已禁用 as four peer statistics at equal visual weight (fontSize 22 w700, _StatItem L126-133), but dashboard_provider.dart:57 defines `int get disabledTasks => totalTasks - enabledTasks`. 已禁用 is never fetched — three of those numbers carry two independent facts.
- Secondary duplication: today's success and failure counts appear as two large numbers in TaskStatsCard (L86-97) and again as the final data point of the 7-day line in TrendChart (trend_chart.dart:126-127), both rendered in the same scroll (dashboard_page.dart:325 and :349).
- Out of my assigned dimension but the most serious thing I found on the dashboard: the page never reads `data.error`. dashboard_provider.dart:129 sets `error: '加载失败'` on failure, but grep for 'error' in dashboard_page.dart matches only the avatar's errorBuilder (L74). On a failed load the page renders CPU 0%, RAM 0%, disk 0%, hostname '-' and os '-' as if they were measurements. AppErrorView already exists (app_state_views.dart:73) and is not used here. Worth routing to whoever owns error/empty states.

## Migrating inline card-shaped BoxDecoration to the AppCard widget  (60 条具体改动)

- AppCard is wired to 6 call sites, not 9. `AppCard(` returns 9 grep hits but 3 are noise: the constructor declaration at app_card.dart:47, and open_api_page.dart:173/:351 which are `_buildAppCard` (a private method, unrelated). All 6 real usages are in one file: system_settings_page.dart:582, 629, 688, 746, 829, 843.
- The '43 处内联卡片形 BoxDecoration' figure in app_card.dart's own docstring (line 7) understates the job. The card-shaped population is 60 of the 131 non-token BoxDecoration sites. 48 are true content cards; 12 more are tinted hint/error banners that AppCard can also carry.
- Bucket B is empty. I could not construct a case for any new AppCard prop. The three candidates all fail on inspection: `boxShadow` would serve only 2 card sites (app_lock_gate:230, log_list_page:736) and Phase 1 wants those shadows gone; `height` would serve only env_list_page:976 and login_page:625, both of which are input/select surfaces (bucket C); `borderWidth` would serve only task_list_page:1816 and env_list_page:1971, which are AnimatedContainers with transforms and cannot be migrated regardless. The right next component is AppNotice (12 sites), which is a new widget, not a new prop.
- '12 boxShadow sites' is not '12 cards with elevation'. Breakdown: 8 are the 32x32 primary-circle add-FAB repeated verbatim across dep_list:1129, env_list:830, notification_list:215, open_api:109, script_list:648, subscription_list:186, task_list:569, user_list:179; 2 are glowing status dots (dashboard_page:442, task_list_page:1855); only 2 are cards. Flattening shadows is mostly a FAB-deduplication task, not an AppCard task — and that repeated FAB is itself a missing shared component (8 identical copies, ~14 lines each).
- Only 1 of the 2 LinearGradients is a card. dashboard_page.dart:407 (_ServerInfoCard) is gradient-filled and migratable; login_page.dart:243 is the full-screen login background with no radius and no border and is bucket C.

## Leftover colour damage from the Phase 0 primary switch (#10B981 emerald -> #409EFF blue), plus WCAG contrast of the semantic pairs actually rendered  (36 条具体改动)

- WRONG: 'dep_list_page 全部 chip still primary' is not a semantic violation. dep_list_page.dart:1208-1214 is the *no-filter* option of `_StatusFilterChip`; the sibling chips already carry semantic colour (已安装 → AppColors.success at :1222, 失败 → AppColors.danger at :1234) and the in-code comment at :1220-1221 says Phase 0 changed 已安装 off primary *specifically so that 全部 could keep brand primary*. Primary-as-selection-accent is correct here. The genuine defect at that chip is contrast (2.48:1 for 12px bold, see items) and an inconsistency: the identically-named `_StatusFilterChip` in log_list_page.dart:847-851 had its default deliberately changed to a neutral colour, so the two pages now disagree about what an unselected-category accent looks like. Pick one.
- WRONG: 'AppColors.termGreen still on the OLD emerald palette' is not leftover brand damage. app_theme.dart:76 is indeed emerald-400 #34D399, but grep for `termGreen` returns exactly two hits: the definition and ansi_text.dart:80, where it fills the ANSI SGR-32 'green' slot of the dark terminal palette. It is the same category as the 28 raw colours you told me to leave alone. Do not touch its value. The only real (cosmetic) issue is that term* lives in app_theme.dart next to the brand ramp, which is what made it look like brand damage; moving termBg/termBgDark/termText/termBlue/termGreen/termRed into ansi_text.dart is a zero-visual-change tidy.
- WRONG: 'batch-action buttons all sharing one colour'. Both batch bars already use four distinct colours — task_list_page.dart:736 slate500 / :744 primary / :762 slate500 / :771 red500, and env_list_page.dart:1059 blue500 / :1066 primary / :1074 slate500 / :1082 red500. The actual defect is narrower and sharper: exactly one button (批量启用) is primary when it should be success, which makes task_list_page.dart:744 and :753 literally the same colour value on two adjacent buttons that do different things (run vs enable).
- PARTLY WRONG framing: the single largest contrast failure in the app is NOT caused by the primary switch. `filledButtonTheme` (app_theme.dart:178-179) and `onPrimary` (:86, :104) put white on #409EFF = 2.78:1, and the login CTA repeats it at login_page.dart:523-524. That is 15px w700 text = normal text, needs 4.5:1, so it fails. But the old emerald #10B981 gave 2.54:1, so the switch *improved* it by 0.24. It is a pre-existing defect the switch inherited, not damage the switch caused. It is still the highest-traffic contrast failure in the app and is worth fixing in Phase 1.
- CONFIRMED as you stated: ansi_text.dart's raw colours are out of scope. Verified all seven consumers of `AnsiTextParser.buildTextSpan` are log renderers (log_stream_page.dart:269, dep_list_page.dart:1637, subscription_list_page.dart:1387 and :1707, script_list_page.dart:2833, panel_log_page.dart:201, task_list_page.dart:2999). Leave the file alone.
- BONUS, outside my dimension but free: lib/features/dashboard/widgets/resource_card.dart is dead code — zero references app-wide (grep for `ResourceCard` returns only its own declaration at :3 and :9). It is also a raw `Card(` consumer. Deleting the file removes one of the 52 `Card(` matches at zero risk.

## Information density (list rows per screen) + AppSnack success/failure feedback  (41 条具体改动)

- "enumerate all 9 current AppSnack call sites" — there are 8, not 9, and none of them is a message. `AppSnack.show` appears at task_list_page.dart:93, log_list_page.dart:265, env-less server_config_page.dart:99, script_list_page.dart:477, script_list_page.dart:1793, dep_list_page.dart:379, backup_page.dart:309, app_lock_settings_page.dart:27. Every one is a one-line `void _showMessage(String) => AppSnack.show(context, message)` wrapper. The real call sites are the 92 `_showMessage(...)` invocations behind them (101 grep hits minus 8 definitions minus 1 doc-comment). Adding `tone` to AppSnack changes zero user-visible messages by itself.
- AppSnack is not the app's snackbar path — it covers well under half of it. 118 raw `showSnackBar(` calls across 17 files bypass it: env_list_page 18, security_page 15, script_list_page 14, open_api_page 12, subscription_list_page 12, user_list_page 10, task_list_page 10, notification_list_page 8, system_settings_page 7, and 8 more files. Two of the six list pages in this brief — env_list_page.dart and subscription_list_page.dart — call AppSnack ZERO times. Phase 0's "replaced the 7 duplicated _showMessage helpers" was true but the migration stopped at those 7 files; "第 1 期应加 tone" is therefore ~10% of the work and the other 90% is migrating raw showSnackBar sites.
- THE ACTUAL FAILURE COUNT: 36 call sites report a failure through the neutral style today (task 4 incl. the `_showActionError` funnel at :95-97 which fans out to ~6 actions; dep 8; log 3; script 7; backup 7; app_lock 6; server_config 1). The single most valuable one-line edit in Part 2 is task_list_page.dart:96 — retagging `_showActionError` turns every task-action failure red at once.
- THE PHASE 0 TOKEN LAYER IS DEAD CODE AS FAR AS THESE PAGES ARE CONCERNED. `AppSpacing` / `AppRadius` / `AppSurfaces` / `context.surfaces` appear in exactly 4 files (31 occurrences): design_tokens.dart itself, app_buttons.dart, app_card.dart, app_state_views.dart. Not one feature page references them. `AppSpacing.listBottom` specifically has ZERO call sites — all six list pages hard-code the literal `100` in `EdgeInsets.fromLTRB(20, 0, 20, 100)`. "Phase 1 is where it actually gets applied" is correct, but the brief's framing that the layer already governs the pages is not.
- "whether AppSpacing.listBottom=100 is still the right value after any density change" is a non-sequitur: listBottom exists to clear a fixed-height nav bar, so row height never enters the calculation and no density change can move it. It IS wrong, for a different reason — three of the six list pages (/scripts app_router.dart:180-183, /deps :197-201, /subscriptions :159-163) are pushed on `_rootNavigatorKey`, outside the ShellRoute, and have no bottom nav bar at all. Their 100dp is 76dp of pure dead space. And on the three that do sit in the shell, the real nav height is 48.5 + MediaQuery.viewPadding.bottom = 72.5dp (gesture) or 96.5dp (3-button), so a flat 100 is 27.5dp too generous on most modern phones and 3.5dp from clipping on the rest.
- THE TASK LIST IS WORSE THAN A CARD COUNT SUGGESTS. `_buildTaskGroup` (task_list_page.dart:1319-1413) emits a 60.9dp collapsible group header unconditionally — `_groupTasks` always produces a bucket, including an empty-key 未分组 one, and line 860 maps every bucket through it. So on a 640dp screen the header chrome (240dp) plus one group header (60.9dp) consumes 300.9dp before the first card, leaving 266.6dp of unobstructed space for 160.1dp cards: exactly ONE task is fully visible. Any density work on task_list that ignores the group header is measuring the wrong thing.
- dep_list_page's row is already close to optimal and should not be a target for aggressive cuts: _DepCard's 78dp is set by the 48dp PopupMenuButton at :1514 (a correct, mandatory tap target) plus 20dp padding plus 8dp margin. Only the 20dp of padding is available (content column is 30.7dp), so the ceiling is -10dp. Do not let a "make every list denser" mandate push this one below 48+2.