# 呆呆面板 APP

呆呆面板 Flutter 客户端，用于在移动端管理面板、任务、脚本、环境变量、订阅、依赖、通知、安全设置与备份恢复。

## 当前版本

- APP：`v1.3.7`
- 适配面板：`v3.2.8`

## 本次适配重点

- 多服务器切换不再需要退出登录：每台面板的登录状态分别保存，切回已登录过的面板直接进首页，两步验证也不用重验。
- 内置安装改用系统 PackageInstaller，不再依赖调起第三方安装器；失败时给出中文原因与重试入口。
- 新增「关于」页：作者主页、面板与 App 两个项目地址、两个问题反馈入口。

## 常见问题

- **登录被面板拒绝（403）**：多见于在群晖、飞牛等 NAS 中通过 Nginx Proxy Manager 或公网域名反代访问的场景。请优先把面板升级到 `v2.3.0` 及以上；升级前可临时在面板 `config.yaml` 的 `cors.origins` 中加入完整公网地址，例如 `https://域名:端口`。

## 开发

```bash
flutter pub get
flutter analyze
flutter test
```
