# 呆呆面板 APP

呆呆面板 Flutter 客户端，用于在移动端管理面板、任务、脚本、环境变量、订阅、依赖、通知、安全设置与备份恢复。

## 当前版本

- APP：`v1.3.8`
- 适配面板：`v3.3.3`

## 本次适配重点

- 任务页可以批量设置通知；日志详情可设置「打开已结束的日志时定位到底部」，与网页端共用同一个账户设置。这两项需要面板 `v3.3.3`，老面板上会提示升级或不显示。
- 镜像源设置与面板对齐（默认源标在腾讯云，补天翼云等，新增「恢复默认」），保存时只提交改动过的字段。
- 拖拽排序松手即保存，不再改写开机任务的执行顺序（需要面板 `v3.2.1`）。

## 常见问题

- **登录被面板拒绝（403）**：多见于在群晖、飞牛等 NAS 中通过 Nginx Proxy Manager 或公网域名反代访问的场景。请优先把面板升级到 `v2.3.0` 及以上；升级前可临时在面板 `config.yaml` 的 `cors.origins` 中加入完整公网地址，例如 `https://域名:端口`。

## 开发

```bash
flutter pub get
flutter analyze
flutter test
```
