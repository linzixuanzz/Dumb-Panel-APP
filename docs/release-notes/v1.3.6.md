# v1.3.6

## 优化

- 运行中的日志只在你停在底部时自动往下跟随；往上翻看时不再被拉回底部，点「回到底部」或滑回底部后恢复跟随（#10）。
- 日志很多时不再卡死：只渲染屏幕附近的行，内存里保留最近 1 万行，超出时顶部会提示，可下载完整日志（#10）。
- 面板日志支持长按选择文字。

## 说明

- 日志页右上角的「自动滚动」开关已去掉，改由滚动位置决定是否跟随。
- 与面板 v3.3.0 同期发布；装在老面板上也能用。
